#include <iostream>
#include <string>

using namespace std;

string UserName(string name;) {

    cout << "Enter Your FullName: " << endl;
    getline(cin, name);
    cout << "\n--------------------------------" << endl;
    return name;
}

int main() {

    UserName(name);

    return 0;
}